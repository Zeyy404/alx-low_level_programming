#include "main.h"

/**
 * _islower - checks for lowercase character
 * Return: 1 if c is lowercase & 0 otherwise
 */
int _islower(int c)
{
	char c;

	if (c > 96 && c < 123)
		_putchar(48);
	else
		_putchar(49);
	_putchar('\n');
	return (0);
}
