#include "main.h"

/**
 * print_number - prints an integer
 * @n: int variable
 * Return: void
 */
void print_number(int n)
{
	if (n < 0)
	{
		_putchar(45);
		n = n * -1;
	}

	while (n > 0)
	{
		_putchar(((n % 10) + 48) * 10);
		n = n / 10;
	}
}
